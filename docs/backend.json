{
  "entities": {
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Represents a user within the CRMI task management system, including their authentication details, role, and departmental affiliation.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the UserProfile entity."
        },
        "name": {
          "type": "string",
          "description": "The full name of the user."
        },
        "email": {
          "type": "string",
          "description": "The email address of the user, used for authentication.",
          "format": "email"
        },
        "role": {
          "type": "string",
          "description": "The assigned role of the user within the system, determining their access permissions.",
          "format": "enum",
          "items": {
            "type": "string"
          }
        },
        "departmentId": {
          "type": "string",
          "description": "Reference to the Department entity the user belongs to. (Relationship: Department 1:N UserProfile)"
        },
        "isApproved": {
          "type": "boolean",
          "description": "Whether the user's registration has been approved by an administrator."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the user profile was created.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name",
        "email",
        "role",
        "departmentId",
        "isApproved",
        "createdAt"
      ]
    },
    "Department": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Department",
      "type": "object",
      "description": "Represents a department within the CRMI organization. Departments are responsible for tasks and define user affiliations.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Department entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the department.",
          "format": "enum",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "id",
        "name"
      ]
    },
    "Task": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Task",
      "type": "object",
      "description": "Represents a task or event managed within the CRMI system, including its details, status, and responsible parties.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Task entity."
        },
        "title": {
          "type": "string",
          "description": "The main title or short name of the task."
        },
        "dateTime": {
          "type": "string",
          "description": "The planned date and time for the task or event.",
          "format": "date-time"
        },
        "place": {
          "type": "string",
          "description": "The physical location where the task will take place."
        },
        "description": {
          "type": "string",
          "description": "A detailed description of the task's objectives and requirements."
        },
        "checklist": {
          "type": "array",
          "description": "A list of sub-items or steps required to complete the task.",
          "items": {
            "type": "string"
          }
        },
        "status": {
          "type": "string",
          "description": "The current progress status of the task.",
          "format": "enum",
          "items": {
            "type": "string"
          }
        },
        "priority": {
          "type": "string",
          "description": "The urgency of the task.",
          "format": "enum",
          "enum": ["не срочно", "срочно"]
        },
        "departmentId": {
          "type": "string",
          "description": "Reference to the Department entity that owns or is responsible for this task. (Relationship: Department 1:N Task)"
        },
        "responsibleUserId": {
          "type": "string",
          "description": "Reference to the UserProfile entity who is designated as responsible for this task. (Relationship: UserProfile 1:N Task)"
        },
        "createdBy": {
          "type": "string",
          "description": "UID of the user who created the task."
        },
        "createdByName": {
          "type": "string",
          "description": "Name of the user who created the task."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the task was initially created.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "title",
        "dateTime",
        "place",
        "description",
        "checklist",
        "status",
        "priority",
        "departmentId",
        "responsibleUserId",
        "createdBy",
        "createdByName",
        "createdAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/userProfiles/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": {
            "$ref": "#/backend/entities/UserProfile"
          },
          "description": "Stores individual user profiles.",
          "params": [
            {
              "name": "userId",
              "description": "The unique ID of the user."
            }
          ]
        }
      },
      {
        "path": "/departments/{departmentId}",
        "definition": {
          "entityName": "Department",
          "schema": {
            "$ref": "#/backend/entities/Department"
          },
          "description": "Contains a fixed list of CRMI departments.",
          "params": [
            {
              "name": "departmentId",
              "description": "The unique identifier for a department."
            }
          ]
        }
      },
      {
        "path": "/tasks/{taskId}",
        "definition": {
          "entityName": "Task",
          "schema": {
            "$ref": "#/backend/entities/Task"
          },
          "description": "A top-level collection for all tasks.",
          "params": [
            {
              "name": "taskId",
              "description": "The unique identifier for a specific task."
            }
          ]
        }
      }
    ]
  }
}
